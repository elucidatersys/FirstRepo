name: First WorkFlow For testing CI
on: workflow_dispatch
jobs:
  jobNameJTest:
       runs-on: ubuntu-latest
       steps:
         - name: Step 1 - Test Case to validate user can navigate to Facebook
           run: echo "First Test will run"
         - name: Step 2 - Once Test case Run completed
           run: echo " Start your tear down process "
